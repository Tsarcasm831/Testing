import { app } from "../app.js";
(function(ns) {
    'use strict';

    ns.pages = ns.pages || {};

    function initUpcomingMusicPage() {}

    Object.assign(ns.pages, { initUpcomingMusicPage });
}(app));
